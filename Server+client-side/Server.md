# Server + client contracts

Firebase integration lives under `Server+client-side/firebase/`:
- `firestore.rules` for access control and anti-cheat constraints.
- `firebase.json` for emulator + function source config.
- `functions/src/index.ts` for callable game logic (`runCommand`).
